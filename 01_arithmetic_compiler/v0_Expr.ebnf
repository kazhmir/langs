Expr := Term {('+' | '-') Term}.
Term ::= Power {('*' | '/') Power}
Power ::= Unary { '^' Unary}
Unary ::= [('+' | '-')] Factor
Factor := '(' Expr ')'
	| number.

// Symbols
number ::= [0-9]+\.?[0-9]*
